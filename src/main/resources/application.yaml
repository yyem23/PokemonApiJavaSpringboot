spring:
  profiles:
    active: local
  application:
    name: pokemon-api
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081